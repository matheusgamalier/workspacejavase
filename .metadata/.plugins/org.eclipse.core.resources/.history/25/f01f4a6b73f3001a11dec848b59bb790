import java.util.Scanner;

import javax.swing.JOptionPane;

public class NomeIdadeMediaEtc {

	public static void main(String[] args) {
		Scanner t = new Scanner(System.in);
		
		String nome = "";
		String nomeMaisVelha = "";
		String nomeMaisNova = "";
		short idade = 0;
		short idadeMaisVelha = 0;
		short idadeMaisNova = 0;
		int qtdePessoas = 1;
		int qtdePessoasMaiores = 0;
		int mediaIdade = 0;
		
		nome = JOptionPane.showInputDialog("Informe o " + qtdePessoas + "º nome").toUpperCase();
		idade = Short.parseShort(JOptionPane.showInputDialog("Informe a idade do(a) " + nome).toUpperCase());
		
		mediaIdade = idade;
		nomeMaisVelha = nome;
		idadeMaisVelha = idade;
		nomeMaisNova = nome;
		idadeMaisNova = idade;
		
		if (idade > 17) {
			qtdePessoasMaiores++;
		}
		
		while (JOptionPane.showInputDialog("Tecle <S> para inserir mais dados").toUpperCase().charAt(0) == 'S') {
			nome = JOptionPane.showInputDialog("Informe o " + qtdePessoas + "º nome").toUpperCase();
			
			idade = Short.parseShort(JOptionPane.showInputDialog("Informe a idade do(a) " + nome).toUpperCase());
			
			qtdePessoas++;
			mediaIdade = (mediaIdade + idade) / qtdePessoas;
			
			if (idade >= idadeMaisVelha) {
				nomeMaisVelha = nome;
				idadeMaisVelha = idade;
			}
			
			if (idade <= idadeMaisNova) {
				nomeMaisNova = nome;
				idadeMaisNova = idade;
			}
			
			if (idade > 17) {
				qtdePessoasMaiores++;
			}
			
		}
		
		System.out.printf("Qtde de pessoas .......: %d \n", qtdePessoas);
		System.out.printf("Qtde de pessoas maiores: %d \n", qtdePessoas);
		System.out.printf("Média das idades ......: %d \n", mediaIdade);
		System.out.printf("Pessoa mais velha .....: %s com %d anos \n", nomeMaisVelha, idadeMaisVelha);
		System.out.printf("Pessoa mais nova  .....: %s com %d anos \n", nomeMaisNova, idadeMaisNova);
			
	}

}
